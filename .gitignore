# --- Build Artifacts (Generated by CMake) ---
build/
out/
bin/
*.exe
*.pdb
*.ilk
*.obj
*.lib
*.dll
*.ini

# --- vcpkg (Dependencies) ---
# We ignore the installed libraries because vcpkg.json will
# re-download them for anyone who clones your repo.
vcpkg_installed/

# We also ignore the vcpkg tool itself since it's a separate repo.
# (Users should clone vcpkg themselves or use a submodule)
vcpkg/

# Exclude log files
logs/

# --- IDE Settings ---
.vscode/
.vs/
*.user

# --- OS Junk ---
.DS_Store
Thumbs.db